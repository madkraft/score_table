{
  "name": "score_table",
  "version": "0.1.0",
  "private": true,
  "devDependencies": {
    "create-react-app-sass": "^1.1.0",
    "react-scripts": "0.8.5",
    "standard": "^8.6.0"
  },
  "dependencies": {
    "material-design-lite": "^1.3.0",
    "react": "^15.4.2",
    "react-dom": "^15.4.2",
    "react-router": "^3.0.2"
  },
  "scripts": {
    "start": "react-scripts-with-sass start",
    "build": "react-scripts-with-sass build",
    "deploy": "npm run build && surge -p build -d whatsthescore.surge.sh",
    "test": "react-scripts test --env=jsdom",
    "eject": "react-scripts eject"
  }
}
